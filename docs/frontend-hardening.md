# Frontend hardening

Finance mobile is the Expo Router reference implementation. This stage adds explicit infrastructure boundaries, public runtime configuration, typed API errors, local/signing artifact protection, and a frontend CI typecheck job.

Local Expo/device validation is intentionally deferred to the development environment.
