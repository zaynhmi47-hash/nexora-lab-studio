# Next step

After this hardening PR is merged, validate Finance mobile with a real Expo install/typecheck/start. Then initialize `apps/dignity/mobile` using the validated Expo Router baseline, adapting its domain boundaries for learning, attendance, SIAKAD, library, and research instead of copying Finance business logic.
