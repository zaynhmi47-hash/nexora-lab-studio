{
  "name": "nexora-monorepo",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "check:boundaries": "node tooling/dependency-boundary-check.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
