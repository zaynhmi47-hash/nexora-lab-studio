packages:
  - "apps/*"
  - "apps/*/web"
  - "apps/*/mobile"
  - "packages/*"

# Keep workspace dependencies isolated. Each package must declare what it imports.
# This prevents accidental dependency access through hoisting.
nodeLinker: isolated
