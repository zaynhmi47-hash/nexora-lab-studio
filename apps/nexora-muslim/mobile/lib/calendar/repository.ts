import type {CalendarPort, HijriEvent} from "./types"; const baseUrl=process.env.EXPO_PUBLIC_NEXORA_CORE_URL??"http://localhost:8000"; export const nexoraCoreCalendarRepository=():CalendarPort=>({events:async()=>{const r=await fetch(baseUrl+"/api/v1/calendar/events/",{headers:{Accept:"application/json"}});if(!r.ok)throw new Error("Calendar request failed ("+r.status+").");return await r.json() as HijriEvent[];}});
