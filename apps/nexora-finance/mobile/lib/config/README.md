# Runtime configuration

Only non-secret client configuration belongs here.

Use `EXPO_PUBLIC_*` variables for values that are safe to ship to the client. Secrets and server credentials must remain outside the mobile bundle and be handled by Nexora Core or trusted provider infrastructure.
